using UnityEngine;

namespace DRAP.Gameplay
{
    public class PlayerSignals : MonoBehaviour
    {
        [SerializeField] CharacterMovementPlayerInput movement;
        [SerializeField] CharacterShooting shooting;

        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {
            
        }
    }
}